import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';

// App component and styles will be added in Sprint 8.
createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <div>
      <h1>Enterprise AI Customer Intelligence Platform</h1>
      <p>Frontend foundation initialized — Sprint 0 complete.</p>
    </div>
  </StrictMode>,
);
